services:
  - type: web
    name: alhabeed
    runtime: node
    plan: free
    buildCommand: npm ci && npm ci --prefix client && npm run build:all
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 22.14.0
